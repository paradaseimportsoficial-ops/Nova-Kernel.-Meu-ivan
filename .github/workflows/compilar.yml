name: Compilar Kernel

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Instalar Ferramentas de Compilação
        run: |
          sudo apt-get update
          sudo apt-get install -y git bc bison flex libssl-dev gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu

      - name: Configurar tarefa
        uses: actions/checkout@v4

      - name: Compilar Imagem do Kernel
        run: |
          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          make mrproper
          # Usamos 'allyesconfig' para tentar forçar uma configuração que cubra as dependências
          # ou, se preferir, podemos tentar pular o defconfig se houver um arquivo de config específico
          make allyesconfig
          make -j2 KCFLAGS="-Wno-error -fpermissive -w"
